# ── Convex ────────────────────────────────────────────────────────────────────
# Convex deployment URL — copied from your Convex dashboard or `npx convex dev` output
VITE_CONVEX_URL=https://<your-deployment>.convex.cloud

# Convex HTTP actions base URL (used for bank OAuth callbacks)
# Same host as above, no trailing slash
VITE_CONVEX_SITE_URL=https://<your-deployment>.convex.site

# Optional: currency symbol shown in the UI (default: CZK)
# VITE_CURRENCY=EUR

# ── Authentication (optional) ─────────────────────────────────────────────────
# Set all three to enable Clerk auth; remove all three to run without login.
# AUTH_PROVIDER=clerk
# VITE_CLERK_PUBLISHABLE_KEY=pk_...
# CLERK_ISSUER_URL=https://your-app.clerk.accounts.dev

# ── Convex server env vars ────────────────────────────────────────────────────
# Non-VITE_ vars below are pushed to Convex via: npm run env:push
#
# LLM_PROVIDER=anthropic
# LLM_MODEL=claude-haiku-4-5
# ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...
# GOOGLE_API_KEY=...
# TOKEN_ENCRYPTION_KEY=<64 hex chars — generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))">
